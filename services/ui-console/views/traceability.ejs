<%- include('layout', { title: 'Traceability', page: 'traceability', body: `

<h1 class="text-glow-cyan">Traceability</h1>

<!-- Stats Row -->
<div class="grid-4 mb-2" style="margin-bottom:1.5rem">
  <div class="stat-card animate-fade-up">
    <div class="stat-icon stat-icon-cyan">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></svg>
    </div>
    <div class="stat-label">Total Traces</div>
    <div class="stat-number" id="stat-traces">-</div>
  </div>
  <div class="stat-card animate-fade-up delay-100">
    <div class="stat-icon stat-icon-emerald">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
    </div>
    <div class="stat-label">Avg Latency</div>
    <div class="stat-number" id="stat-latency">-</div>
  </div>
  <div class="stat-card animate-fade-up delay-200">
    <div class="stat-icon stat-icon-violet">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"/></svg>
    </div>
    <div class="stat-label">Tool Calls</div>
    <div class="stat-number" id="stat-tool-calls">-</div>
  </div>
  <div class="stat-card animate-fade-up delay-300">
    <div class="stat-icon" style="background:linear-gradient(135deg,rgba(244,63,94,0.15),rgba(244,63,94,0.08));color:#f43f5e">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><line x1="15" y1="9" x2="9" y2="15"/><line x1="9" y1="9" x2="15" y2="15"/></svg>
    </div>
    <div class="stat-label">Errors</div>
    <div class="stat-number" id="stat-errors">-</div>
  </div>
</div>

<!-- Langfuse Dashboard (Native) -->
<div class="card animate-fade-up" style="margin-bottom:1.5rem">
  <div class="section-header mb-1">
    <div style="display:flex;align-items:center;gap:1rem">
      <div style="width:40px;height:40px;border-radius:10px;background:linear-gradient(135deg,rgba(244,63,94,0.15),rgba(244,63,94,0.08));display:flex;align-items:center;justify-content:center">
        <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="#f43f5e" stroke-width="2"><path d="M12 2a9 9 0 0 1 9 9c0 3.9-3.1 7.1-5 8.9a13 13 0 0 1-4 2.1 13 13 0 0 1-4-2.1C6.1 18.1 3 14.9 3 11a9 9 0 0 1 9-9z"/><circle cx="12" cy="11" r="3"/></svg>
      </div>
      <div>
        <h3 style="margin:0">Langfuse Dashboard</h3>
        <p style="margin:0.25rem 0 0;color:var(--text-3);font-size:0.82rem">Tracing, cost analysis & latency breakdown — data via Langfuse API</p>
      </div>
    </div>
    <a href="${urls.langfuse}" target="_blank" class="btn btn-primary btn-sm">
      <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2"><path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"/><polyline points="15 3 21 3 21 9"/><line x1="10" y1="14" x2="21" y2="3"/></svg>
      Open Full Langfuse
    </a>
  </div>

  <!-- Langfuse KPI row -->
  <div style="display:grid;grid-template-columns:repeat(4,1fr);gap:0.75rem;margin-bottom:1rem">
    <div style="background:var(--glass-bg);border:1px solid var(--glass-border);border-radius:12px;padding:0.75rem 1rem;text-align:center">
      <div style="font-size:0.72rem;color:var(--text-3);margin-bottom:0.25rem">Total Tokens</div>
      <div style="font-size:1.15rem;font-weight:700;color:var(--text-1)" id="lf-tokens">-</div>
    </div>
    <div style="background:var(--glass-bg);border:1px solid var(--glass-border);border-radius:12px;padding:0.75rem 1rem;text-align:center">
      <div style="font-size:0.72rem;color:var(--text-3);margin-bottom:0.25rem">Avg Tokens/Trace</div>
      <div style="font-size:1.15rem;font-weight:700;color:var(--text-1)" id="lf-avg-tokens">-</div>
    </div>
    <div style="background:var(--glass-bg);border:1px solid var(--glass-border);border-radius:12px;padding:0.75rem 1rem;text-align:center">
      <div style="font-size:0.72rem;color:var(--text-3);margin-bottom:0.25rem">Success Rate</div>
      <div style="font-size:1.15rem;font-weight:700;color:#10b981" id="lf-success">-</div>
    </div>
    <div style="background:var(--glass-bg);border:1px solid var(--glass-border);border-radius:12px;padding:0.75rem 1rem;text-align:center">
      <div style="font-size:0.72rem;color:var(--text-3);margin-bottom:0.25rem">P95 Latency</div>
      <div style="font-size:1.15rem;font-weight:700;color:#f59e0b" id="lf-p95">-</div>
    </div>
  </div>

  <!-- Latency distribution bar -->
  <div style="margin-bottom:1rem">
    <div style="font-size:0.78rem;font-weight:600;color:var(--text-2);margin-bottom:0.5rem">Latency Distribution</div>
    <div id="lf-latency-bars" style="display:flex;gap:2px;height:40px;align-items:flex-end;border-radius:6px;overflow:hidden"></div>
    <div style="display:flex;justify-content:space-between;font-size:0.65rem;color:var(--text-3);margin-top:0.25rem">
      <span>&lt;1s</span><span>1-3s</span><span>3-5s</span><span>5-10s</span><span>10s+</span>
    </div>
  </div>

  <!-- Trace Timeline -->
  <div>
    <div style="font-size:0.78rem;font-weight:600;color:var(--text-2);margin-bottom:0.5rem">Trace Timeline</div>
    <div id="lf-timeline" style="display:flex;flex-direction:column;gap:0.35rem;max-height:180px;overflow-y:auto"></div>
  </div>
</div>

<!-- Recent Traces -->
<div class="card animate-fade-up delay-100" style="margin-bottom:1.5rem">
  <div class="section-header">
    <h3>Recent Traces</h3>
    <button class="btn btn-secondary btn-sm" onclick="loadTraces()">Refresh</button>
  </div>
  <div id="traces-table" class="mt-1" style="color:var(--text-3);font-size:0.82rem">Loading...</div>
</div>

<!-- Trace Detail -->
<div class="card animate-fade-up delay-200" id="trace-detail-card" style="display:none">
  <div class="section-header">
    <h3>Trace Detail</h3>
    <button class="btn btn-secondary btn-sm" onclick="document.getElementById('trace-detail-card').style.display='none'">Close</button>
  </div>
  <div id="trace-detail" class="mt-1 mono" style="font-size:0.78rem;line-height:1.6"></div>
</div>

<script>
function loadTraces() {
  fetch('/api/traces').then(r=>r.json()).then(d=>{
    const el = document.getElementById('traces-table');
    const traces = d.traces || [];

    // Update top stats
    document.getElementById('stat-traces').textContent = traces.length;
    const durations = traces.filter(t=>t.latency!=null).map(t=>t.latency);
    const avgMs = durations.length ? durations.reduce((a,b)=>a+b,0)/durations.length : 0;
    document.getElementById('stat-latency').textContent = durations.length
      ? (avgMs/1000).toFixed(1) + 's' : '-';

    let toolCount = 0;
    traces.forEach(t => {
      if (t.observations) {
        toolCount += t.observations.filter(o => o.type === 'SPAN' && (o.name||'').toLowerCase().includes('tool')).length;
      }
      if (toolCount === 0 && t.output && typeof t.output === 'string' && t.output.includes('tool')) toolCount++;
    });
    document.getElementById('stat-tool-calls').textContent = toolCount;

    const errorTraces = traces.filter(t=>t.status==='ERROR');
    document.getElementById('stat-errors').textContent = errorTraces.length;

    // Token stats
    let totalTokens = 0, tracesWithTokens = 0;
    traces.forEach(t => {
      const usage = t.usage || t.totalUsage || {};
      const toks = (usage.totalTokens || usage.total || 0);
      if (toks > 0) { totalTokens += toks; tracesWithTokens++; }
    });
    document.getElementById('lf-tokens').textContent = totalTokens > 0 ? totalTokens.toLocaleString() : '-';
    document.getElementById('lf-avg-tokens').textContent = tracesWithTokens > 0 ? Math.round(totalTokens/tracesWithTokens).toLocaleString() : '-';

    // Success rate
    const successCount = traces.filter(t => t.status !== 'ERROR').length;
    document.getElementById('lf-success').textContent = traces.length > 0
      ? Math.round(successCount/traces.length*100) + '%' : '-';
    document.getElementById('lf-success').style.color = traces.length > 0 && successCount/traces.length < 0.9 ? '#f43f5e' : '#10b981';

    // P95 latency
    if (durations.length > 0) {
      const sorted = [...durations].sort((a,b)=>a-b);
      const p95idx = Math.min(Math.floor(sorted.length * 0.95), sorted.length-1);
      document.getElementById('lf-p95').textContent = (sorted[p95idx]/1000).toFixed(1) + 's';
    }

    // Latency distribution bars
    const buckets = [0,0,0,0,0];
    durations.forEach(ms => {
      const s = ms/1000;
      if (s < 1) buckets[0]++;
      else if (s < 3) buckets[1]++;
      else if (s < 5) buckets[2]++;
      else if (s < 10) buckets[3]++;
      else buckets[4]++;
    });
    const maxBucket = Math.max(...buckets, 1);
    const colors = ['#10b981','#06b6d4','#f59e0b','#f97316','#f43f5e'];
    document.getElementById('lf-latency-bars').innerHTML = buckets.map((b,i) =>
      '<div style="flex:1;background:' + colors[i] + ';height:' + Math.max(4, b/maxBucket*40) + 'px;border-radius:3px;opacity:0.85;position:relative" title="' + b + ' traces"><span style="position:absolute;top:-14px;left:50%;transform:translateX(-50%);font-size:0.6rem;color:var(--text-3)">' + b + '</span></div>'
    ).join('');

    // Timeline
    const timelineEl = document.getElementById('lf-timeline');
    if (traces.length === 0) {
      timelineEl.innerHTML = '<div style="color:var(--text-3);font-size:0.78rem;padding:0.5rem">No traces yet — run an agent prompt to generate data</div>';
    } else {
      timelineEl.innerHTML = traces.slice(0, 12).map(t => {
        const dur = t.latency ? (t.latency/1000).toFixed(1) + 's' : '-';
        const time = t.timestamp ? new Date(t.timestamp).toLocaleTimeString() : '-';
        const statusColor = t.status === 'ERROR' ? '#f43f5e' : '#10b981';
        const barWidth = t.latency && durations.length ? Math.max(8, Math.min(100, t.latency / Math.max(...durations) * 100)) : 20;
        return '<div style="display:flex;align-items:center;gap:0.5rem;font-size:0.75rem">' +
          '<span style="width:6px;height:6px;border-radius:50%;background:' + statusColor + ';flex-shrink:0"></span>' +
          '<span style="width:60px;flex-shrink:0;color:var(--text-3);font-family:monospace;font-size:0.68rem">' + time + '</span>' +
          '<span style="flex:1;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;color:var(--text-2)">' + escapeHtml(t.name||t.id?.slice(0,12)||'-') + '</span>' +
          '<div style="width:100px;height:8px;background:var(--glass-bg);border-radius:4px;overflow:hidden;flex-shrink:0"><div style="width:' + barWidth + '%;height:100%;background:' + (t.latency > 5000 ? '#f59e0b' : '#06b6d4') + ';border-radius:4px"></div></div>' +
          '<span style="width:40px;text-align:right;font-family:monospace;font-size:0.68rem;color:var(--text-3);flex-shrink:0">' + dur + '</span>' +
          '</div>';
      }).join('');
    }

    // Traces table
    if (traces.length === 0) {
      el.innerHTML = '<p style="color:var(--text-3)">' + (d.error || 'No traces found. Run an agent prompt to generate traces.') + '</p>';
      return;
    }
    el.innerHTML = '<table><tr><th>Trace ID</th><th>Name</th><th>Session</th><th>Input</th><th>Latency</th><th>Status</th><th></th></tr>' +
      traces.map(t => {
        const latStr = t.latency ? (t.latency/1000).toFixed(2) + 's' : '-';
        const statusBadge = t.status === 'ERROR'
          ? '<span style="color:#f43f5e;font-size:0.72rem">&#x2716; Error</span>'
          : '<span style="color:#10b981;font-size:0.72rem">&#x2714; OK</span>';
        return '<tr>' +
        '<td class="mono" style="font-size:0.78rem">' + escapeHtml((t.id||'').slice(0,12)) + '...</td>' +
        '<td>' + escapeHtml(t.name||'-') + '</td>' +
        '<td class="mono" style="font-size:0.78rem">' + escapeHtml((t.sessionId||'-').slice(0,12)) + '</td>' +
        '<td style="max-width:200px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap">' + escapeHtml(t.input ? (typeof t.input==='string'?t.input:JSON.stringify(t.input)).slice(0,60) : '-') + '</td>' +
        '<td class="mono" style="font-size:0.78rem">' + latStr + '</td>' +
        '<td>' + statusBadge + '</td>' +
        '<td><button class="btn btn-secondary btn-sm" onclick="viewTrace(\\'' + escapeHtml(t.id) + '\\')">Detail</button></td>' +
        '</tr>';
      }).join('') + '</table>';
  }).catch(e=>{
    document.getElementById('traces-table').innerHTML = '<p style="color:var(--danger)">Failed to load traces</p>';
  });
}

function viewTrace(traceId) {
  document.getElementById('trace-detail-card').style.display = 'block';
  document.getElementById('trace-detail').innerHTML = '<p style="color:var(--text-3)">Loading trace...</p>';

  fetch('/api/traces/' + traceId).then(r=>r.json()).then(d=>{
    const t = d.trace || d;
    let html = '<div style="display:grid;grid-template-columns:140px 1fr;gap:0.5rem 1rem;margin-bottom:1rem">';
    html += '<span style="color:var(--text-3)">Trace ID</span><span>' + escapeHtml(t.id||'-') + '</span>';
    html += '<span style="color:var(--text-3)">Name</span><span>' + escapeHtml(t.name||'-') + '</span>';
    html += '<span style="color:var(--text-3)">Session</span><span>' + escapeHtml(t.sessionId||'-') + '</span>';
    html += '<span style="color:var(--text-3)">Timestamp</span><span>' + (t.timestamp ? new Date(t.timestamp).toLocaleString() : '-') + '</span>';
    html += '<span style="color:var(--text-3)">Latency</span><span>' + (t.latency ? (t.latency/1000).toFixed(2) + 's' : '-') + '</span>';
    html += '<span style="color:var(--text-3)">Input</span><span style="white-space:pre-wrap;max-height:120px;overflow-y:auto;display:block">' + escapeHtml(t.input ? (typeof t.input==='string'?t.input:JSON.stringify(t.input,null,2)).slice(0,500) : '-') + '</span>';
    html += '<span style="color:var(--text-3)">Output</span><span style="white-space:pre-wrap;max-height:200px;overflow-y:auto;display:block">' + escapeHtml(t.output ? (typeof t.output==='string'?t.output:JSON.stringify(t.output,null,2)).slice(0,1000) : '-') + '</span>';
    html += '</div>';

    if (d.observations && d.observations.length > 0) {
      html += '<h4 style="margin:1rem 0 0.5rem;color:var(--text-1)">Steps</h4>';
      html += '<div style="border-left:2px solid var(--accent);padding-left:1rem">';
      d.observations.forEach(obs => {
        const icon = obs.type === 'GENERATION' ? '&#x1F9E0;' : (obs.name||'').includes('tool') ? '&#x1F527;' : '&#x1F4CD;';
        const dur = obs.endTime && obs.startTime ? ((new Date(obs.endTime) - new Date(obs.startTime))/1000).toFixed(2) + 's' : '';
        html += '<div style="margin-bottom:0.75rem;padding:0.5rem;background:var(--glass-bg);border-radius:8px">';
        html += '<div style="display:flex;justify-content:space-between;align-items:center">';
        html += '<span>' + icon + ' <strong>' + escapeHtml(obs.name||obs.type||'-') + '</strong></span>';
        html += '<span class="badge" style="font-size:0.7rem">' + dur + '</span>';
        html += '</div>';
        if (obs.input) html += '<div style="margin-top:0.25rem;font-size:0.75rem;color:var(--text-3)">Input: ' + escapeHtml((typeof obs.input==='string'?obs.input:JSON.stringify(obs.input)).slice(0,200)) + '</div>';
        if (obs.output) html += '<div style="margin-top:0.25rem;font-size:0.75rem;color:var(--text-2)">Output: ' + escapeHtml((typeof obs.output==='string'?obs.output:JSON.stringify(obs.output)).slice(0,300)) + '</div>';
        html += '</div>';
      });
      html += '</div>';
    }

    document.getElementById('trace-detail').innerHTML = html;
  }).catch(e => {
    document.getElementById('trace-detail').innerHTML = '<p style="color:var(--danger)">Failed to load trace detail: ' + escapeHtml(e.message) + '</p>';
  });
}

loadTraces();
</script>
` }) %>
